#pragma once
#include <GameState.h>

namespace dae
{
    class DeathScreen : public GameState
    {
    public:
        DeathScreen() = default;
        ~DeathScreen() override = default;

    private:
    };
}

